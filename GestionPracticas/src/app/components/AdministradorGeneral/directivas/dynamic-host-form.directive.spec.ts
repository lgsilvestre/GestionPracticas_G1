import { DynamicHostFormDirective } from './dynamic-host-form.directive';

describe('DynamicHostFormDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicHostFormDirective();
    expect(directive).toBeTruthy();
  });
});
